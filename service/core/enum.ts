export enum Platform {
  Android = 'Android',
  iOS = 'iOS',
};

export enum ReleaseType {
  Alpha = 'Alpha',
  Beta = 'Beta',
};

export enum BuildType {
  Debug = 'Debug',
  Release = 'Release',
};

export enum WebhookType {
  URL = 'URL',
}
